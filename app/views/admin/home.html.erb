<div id="content-header">
  <h1><%= t('welcome_admin') %></h1>
  <h3><%= t('good_day') %></h3>
  <div id="app-back-button">
  </div>
</div>
<div id="inner-tab-menu">
  <ul>
    <li><%= link_to t('view_all'), :controller=>"admin", :action=>"all_subdomains" %></li>
  </ul>
</div>

<div id="page-yield">

  <% form_for @school, :url => {:controller => 'admin', :action => 'abc'} do |f| %>

      <%= render :partial => 'school_form' , :locals => {:f => f} %>
  <% end %>
</div>